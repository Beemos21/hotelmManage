# from django.apps import AppConfig
#
#
# class bookingConfig(AppConfig): name = 'full.python.path.to.your.app.booking' label = 'my.booking'  # <-- this is
# the important line - change it to anything other than the default, which is the # module name ('foo' in this case)
#
#
# default_app_config = 'full.python.path.to.your.app.booking.apps.bookingConfig'
